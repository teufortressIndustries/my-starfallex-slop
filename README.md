# my-starfallex-slop
my starfallex gmod slop scripts

## [prop_disguise_extra.txt](https://github.com/teufortressIndustries/my-starfallex-slop/blob/main/prop_disguise_extra.txt)
WASD to walk Space to jump, zoom key to copy prop, alt1 to enable physprop mode, alt2 to freeze physprop
you can also change with a chat command, example:
`!prop models/props_junk/watermelon01.mdl`
or change directly in script
`local currentModel = "models/maxofs2d/hover_rings.mdl"` > `local currentModel = "models/props_junk/watermelon01.mdl`
